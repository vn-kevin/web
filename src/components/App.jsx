import React, { Component, PropTypes } from 'react';
import {Link } from 'react-router';

const App = ({ location }) => {
  return (
   	<div>
   		Day建造
   	</div>
  );
};

App.propTypes = {
};

export default App;
